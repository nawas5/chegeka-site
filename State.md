2026-04-18: SSH новый `~/.ssh/id_ed25519_chegeka`, config `Host github.com` IdentitiesOnly. Удалены `deploy_my_vibe*`, `id_ed25519_github*`. Git init, remote `git@github.com:nawas5/chegeka-site.git`. Ключ `chegeka-site` добавлен в GitHub; `ssh -T git@github.com` — OK. Git: `core.sshCommand` → системный `OpenSSH\\ssh.exe` (обход MSYS + кириллица в USERPROFILE). `~/.ssh/config`: IdentityFile/UserKnownHostsFile через короткий путь `C:/Users/73B5~1/.ssh/...`. Pull `--allow-unrelated-histories` + README, push main OK.
